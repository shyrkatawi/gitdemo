/* Напишите функцию, которая принимает 1 параметр при 1 вызове, она его запоминает , при втором--
Умножает переданный параметр с тем, что пердали первый раз и тд .
Например: 
const mul3 = mul(3)
const mul15 = mul3(5)//15
mul15(2)//30; т.е. должна вернуться некая другая функция, которую мы можем вызвать с аргументом */